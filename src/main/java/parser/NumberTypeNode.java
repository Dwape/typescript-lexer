package parser;

// Is this a little exaggerated?
// Maybe we have a little too many nodes and node types
// It ensures type safety, but is it really worth it?
public class NumberTypeNode implements TypeNode {

    public NumberTypeNode() {}
}
